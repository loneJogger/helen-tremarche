class Cell {
  constructor(imagePath, gType, speed, startX, range, gDef) {
    this.imagePath = imagePath
    this.gType = gType
    this.speed = speed
    this.startX = startX
    this.range = range
    this.gDef = gDef
  }
}

export default Cell
